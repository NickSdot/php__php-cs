--- old.phpt
+++ new.phpt
@@ -16,19 +16,19 @@
 	try {
 		$d1 = DateTime::createFromFormat('!m/d/Y', $string);
 	} catch (ValueError $v) {
-		echo $v::class, ": ", $v->getMessage(), PHP_EOL;
+		echo $v::class, ': ', $v->getMessage(), \PHP_EOL;
 	}
 
 	try {
 		$d2 = DateTimeImmutable::createFromFormat('!m/d/Y', $string);
 	} catch (ValueError $v) {
-		echo $v::class, ": ", $v->getMessage(), PHP_EOL;
+		echo $v::class, ': ', $v->getMessage(), \PHP_EOL;
 	}
 
 	try {
 		$d3 = date_parse_from_format('m/d/Y', $string);
 	} catch (ValueError $v) {
-		echo $v::class, ": ", $v->getMessage(), PHP_EOL;
+		echo $v::class, ': ', $v->getMessage(), \PHP_EOL;
 	}
 
 	var_dump($d1, $d2, $d3);
